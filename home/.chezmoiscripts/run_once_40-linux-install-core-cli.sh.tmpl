{{- if ne .chezmoi.os "linux" -}}{{""}}{{- else -}}
#!/usr/bin/env bash
set -euo pipefail

sudo apt-get update
sudo apt-get install -y \
  curl \
  unzip \
  ca-certificates \
  jq

jq --version
echo "OK: core CLI installed"
{{- end -}}
