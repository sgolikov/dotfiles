{{- if ne .chezmoi.os "darwin" -}}{{""}}{{- else -}}
#!/usr/bin/env bash
set -euo pipefail

command -v brew >/dev/null 2>&1 || {
  echo "Homebrew is required. Install it first." >&2
  exit 1
}

brew install jq unzip

jq --version
echo "OK: core CLI installed (jq, unzip)"
{{- end -}}
