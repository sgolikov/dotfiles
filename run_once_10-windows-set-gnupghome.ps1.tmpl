{{- if ne .chezmoi.os "windows" -}}
exit 0
{{- end -}}

$target = "$env:USERPROFILE\.gnupg"

# Set per-user env var (persisted)
[Environment]::SetEnvironmentVariable("GNUPGHOME", $target, "User")

Write-Host "GNUPGHOME set to $target (User). Restart terminals/IDE to take effect."
